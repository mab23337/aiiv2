services:
  - type: web
    name: amazing-image-identifier
    env: docker
    plan: starter
    region: virginia
    healthCheckPath: /health
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: HF_API_TOKEN
        sync: false   # You'll paste this in manually in the Render dashboard
    autoDeploy: true
